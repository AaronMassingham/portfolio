import styled from "styled-components";

const Logo = () => {
	return <Container></Container>;
};

const Container = styled.div`
	max-width: 4rem;
	width: 100%;
	max-height: 4rem;
	aspect-ratio: 1/1;
`;

export default Logo;
